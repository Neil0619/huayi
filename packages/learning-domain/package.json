{
  "name": "@huayi/learning-domain",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "test": "pnpm --dir ../.. exec vitest run --config vitest.config.ts --project learning-domain",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.test.json"
  },
  "dependencies": {
    "wink-lemmatizer": "3.0.4",
    "zod": "^4.0.5"
  }
}
