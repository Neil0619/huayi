{
  "name": "@huayi/cloud-contracts",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "test": "pnpm --dir ../.. exec vitest run --config vitest.config.ts --project cloud-contracts",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.test.json"
  },
  "dependencies": {
    "@huayi/learning-domain": "workspace:*",
    "zod": "^4.0.5"
  }
}
