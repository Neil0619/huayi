{
  "name": "@huayi/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "test": "pnpm --dir ../.. exec vitest run --config vitest.config.ts --project api",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.test.json"
  },
  "dependencies": {
    "@huayi/cloud-contracts": "workspace:*",
    "@huayi/learning-domain": "workspace:*",
    "@supabase/supabase-js": "2.112.3",
    "hono": "^4.9.8",
    "postgres": "3.4.9",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.4"
  }
}
