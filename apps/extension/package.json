{
  "name": "@huayi/extension",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --mode content && vite build --mode background",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.test.json && tsc --noEmit --project tsconfig.config.json"
  },
  "dependencies": {
    "@huayi/protocol": "workspace:*"
  }
}
