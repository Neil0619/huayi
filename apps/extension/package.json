{
  "name": "@huayi/extension",
  "version": "0.13.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --mode content && vite build --mode youtube-bridge && vite build --mode options && vite build --mode popup && vite build --mode background",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.test.json && tsc --noEmit --project tsconfig.config.json"
  },
  "dependencies": {
    "@huayi/protocol": "workspace:*"
  }
}
