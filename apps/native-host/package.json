{
  "name": "@huayi/native-host",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.config.ts --mode host && vite build --config vite.config.ts --mode installer && vite build --config vite.config.ts --mode diagnostics && vite build --config vite.config.ts --mode compatible-smoke",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.test.json"
  },
  "dependencies": {
    "@huayi/protocol": "workspace:*",
    "zod": "^4.0.5"
  }
}
