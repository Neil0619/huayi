{
  "name": "@huayi/native-host",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.config.ts --mode host && vite build --config vite.config.ts --mode installer",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.test.json"
  },
  "dependencies": {
    "@huayi/protocol": "workspace:*"
  }
}
