# 依赖安全修复计划（2026-09-22）

## 目标与边界

修复完整依赖审计确认的安装、构建和测试供应链问题，并重新核对锁文件与本机实际安装。
审计基线为 `8b341f9a1f5691e8dd6088f09c226ee3ee2c3a0b`：锁文件 37 项命中，
实际安装 42 项命中，pnpm 10.12.4 自身另有 26 条公告。数字不是线上可利用漏洞数。
原始证据保存在本地 `artifacts/dependency-audit-20260922/`。

本次不提交、推送、部署或安装产品，不调用真实模型、修改云端配置或数据库。
已有 Store 域名管理修改保持原状。共享工具变化覆盖 macOS 与 Windows 的开发流程；
macOS 本机验证与尚未执行的 Windows 验证分别记录，不能相互替代。

## 执行步骤

1. **安装基础**：校验来源及完整性后使用 pnpm 10.34.5，更新项目 pin 和安装说明。
   保存开始快照，建立持续可见的安装日志。用准确锁文件重建依赖并核对真实解析，
   消除 Swiper、PostCSS、nanoid 的安装漂移。
2. **兼容补丁**：PostCSS 8.x 更新到 8.5.28、qs 到 6.16.0、js-yaml 4.x 到 4.3.2、
   glob 10.x 到 10.5.0、adm-zip 到 0.6.1。限制 override 范围并验证直接消费者。
3. **测试工具**：Vitest 与 coverage-v8 同步迁移到 4.1.11，明确 Node 开发环境底线，
   保留覆盖率范围和阈值；运行完整离线测试与 Store 覆盖率。
4. **Taro 消费者**：检查并替换旧模板下载/解压链和无维护包；针对实际调用迁移
   PostCSS 7、XML minifier、WDS、esbuild、serialize-javascript 等。
   跨主版本变化必须有消费者行为验证，不能仅改版本、隐藏告警或关闭功能。
5. **回归与审计门禁**：把合成恶意归档、CSS source map 等审计样例转为安全回归；
   完整审计覆盖开发依赖、运行依赖、包管理器和真实安装状态。
   若公告范围误报仍存在，逐条保留版本、输入前提、源码和可复现证据。
6. **收尾**：核对所有风险处置、用户改动哈希与最终 diff；执行相关格式、lint、
   类型检查、全部离线单测、受影响产品构建及必要独立检查。
   回执逐项区分通过、失败和未运行，不把未部署产物描述为线上已修复。

## 验收条件

- 包管理器精确版本无已知公告；冻结安装成功，实际依赖与锁文件一致。
- 原始审计中的每条公告都有修复版本或经过验证的处置依据，不使用忽略列表制造通过。
- 正常模板提取、CSS/XML 处理和现有构建行为保持可用；攻击样例不能越界读写。
- 测试迁移不删除断言、不降低覆盖率；Web/API、Store、Classic/Host、小程序构建通过。
- 新工具链的验证记录可以定位到最终文件快照；Windows 实机证据缺失时明确保留该限制。

## 执行记录

状态：**修复与 macOS 验证完成；Windows 按用户要求延期**。
Windows 实机验证按用户 2026-09-22 的明确要求暂不进行；
Windows 保留为后续验证项，不声称双平台门禁完成。

- 步骤 1–5 已完成：项目执行 pnpm 10.34.5；冻结安装、实际依赖/补丁核对、完整与生产审计
  均通过，当前已知公告为 0。没有使用忽略列表。
- 步骤 6 的 macOS 检查已执行：最终完整离线单测 5,889 项通过、12 项既有跳过；
  包含 36 项安全/工具链回归和新增 2 项密钥/记录身份回归。10 个工作区构建通过，最后修订的
  小程序构建单独重跑通过。类型检查、改动文件格式/Lint、架构及包边界检查通过。
- Store 覆盖率 1,035 项测试通过，语句 89.97%、分支 85.49%、函数 89.58%、行 91.87%；
  四项原有 85% 门槛保持不变。
- Playwright 共 231 项：224 通过、7 项截图基线比对失败。这 7 项用原锁文件在隔离目录
  全部复现；25 张实际截图中 20 张与修复候选字节一致，其余尺寸一致、相差 8–101 像素。
  记录为既有截图问题，不更新基线、不将失败标为通过。
- 独立复核发现的归档链接边界、独立分包异步 XML 输出、实际补丁缺失的审计漏报，
  以及 HTTP/2 URL 方案均已修复并有回归。早先被宿主安全检查中断的复核保留原失败记录，
  后续通过只读源码、真实依赖图和普通离线兼容检查补足独立复核。
- 后续复核补充的 Node 最低版本问题已修正：22 LTS 下限统一为 22.13；全部锁定依赖的
  Node engines 范围均兼容 22.13/24/26，未声称执行 Node 22 实机验证。
- 最终全量测试发现 H5 测试的中文日志触发 Node 测试运行器通信错误。定位后仅将该测试
  加载模块的日志转向 stderr，全部断言保留；连续 20 轮共 80 项通过，随后完整单测通过。
- 未执行 Windows CI/SEA/实机验收，未执行真实模型、凭据或产品安装检查；未提交、推送或部署。
  五个既有用户修改文件的 SHA-256 均未变化。

逐条公告处置、检查输出、截图对照和交接命令保存在本地
`artifacts/dependency-remediation-20260922/report-final.md`、`receipt.json` 及相邻 JSON/日志。
早先的 `report.md` 和 `receipt-before-review-continuation.json` 仅保留历史检查状态。
