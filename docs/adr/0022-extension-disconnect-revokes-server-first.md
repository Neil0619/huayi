# Extension 断开先撤销服务器会话

Store Extension 的“断开此设备”先用当前 DeviceSession token 调用只能撤销自身的服务器端点，收到统一
204 后才清除本机会话和账号绑定队列。我们拒绝先忘记本机 token 的旧行为，因为断网或丢包后客户端将
永久失去撤销仍有效服务器凭证的能力；代价是离线时安全断开必须保留原状态并提示联网重试，用户仍可从
Web 设备页远程撤销。
