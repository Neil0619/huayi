---
status: accepted
---

# 付费生成必须先持久化，模型发出后不得透明重领

每次平台模型调用必须先建立持久 PlatformGeneration 并取得额度预留，Provider dispatch 前的过期任务
可以安全接管；dispatch 后若失去 worker，则按最坏预留保守结算并进入终态，只有用户显式发起新的生成
才能再次调用模型。这样牺牲了崩溃时的自动无感重试，但避免旧 worker 仍可能产生费用时由新 worker
重复调用和重复收费；已经取得并严格校验的模型输出必须先耐久保存，再交给领域事务应用。
