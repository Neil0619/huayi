# Windows 桌面入口闪退修复回执

2026-09-25。影响 Windows 打开器安装和迁移；扩展、媒体处理、回环服务及 Classic Host 未改动。

## 根因与复现

旧安装根为 `%LOCALAPPDATA%\SeenSaid\asbplayer-opener`。本机 Codex 由 MSIX 应用运行，
写入被重定向到其 `LocalCache\Local\SeenSaid\asbplayer-opener`。继承开发应用环境的命令能读到
这些文件；普通资源管理器却看不到逻辑安装路径。

实际从资源管理器启动旧快捷方式时，PowerShell 在执行脚本前以 `-196608` 退出，原始错误是
`-File` 参数的脚本不存在。原生进程记录确认启动父进程为资源管理器；其环境下的独立文件探针
确认正常 AppData 安装目录不存在、应用私有目录存在。只执行 `powershell -Command exit` 成功。
这区分了路径不可见与 PowerShell 本身不可用，也解释了此前开发进程直接启动的假阳性。

该行为符合微软的 [MSIX 文件虚拟化说明](https://learn.microsoft.com/en-us/windows/msix/desktop/flexible-virtualization)：
AppData 新文件可写入应用私有位置，用户目录中 AppData 之外的可写位置不受这一规则影响。
本次根因以本机两个启动环境的实际对照为依据，没有更改 MSIX 沙盒或系统安全设置。

此前 [选片窗口回执](asbplayer-windows-picker-validation.md) 只解决窗口所有者问题；
其中“最初红色闪退未复现”的状态由本次证据接续，不把旧测试改写为已覆盖本故障。

## 修复

- 固定安装根改为 `%USERPROFILE%\SeenSaid\asbplayer-opener`，不依赖 AppData 虚拟化或开发应用包名。
- 首次迁移保留旧配置；新安装已有配置时以新配置为准。旧文件与媒体缓存保留。
- 仅迁移目标程序和参数均匹配本工具的旧快捷方式，拒绝覆盖无关同名入口。
- 本机实际目录为 `C:\Users\niu06\SeenSaid\asbplayer-opener`；桌面名称仍为「语见本机视频」。
  扩展仍使用 `E:\Document\huayi\apps\store-extension\dist`，没有重新构建或更换扩展身份。

## 验证

- TDD 安装路径回归先失败于仍返回 AppData 路径，修复后通过。
- 原生快捷方式迁移回归先失败于旧入口不能迁移，修复后实际通过 `.lnk` 执行生成的 PowerShell，
  Node 收到准确配置路径；同时检查无关入口未被替换。
- 本机打开器、安装、原生窗口、媒体与回环服务共 10 项定向测试通过，0 跳过。
- 安装后新旧配置内容一致，旧配置哈希未变，原缓存目录仍存在。
- 修复后通过界面实际双击桌面文件夹中的快捷方式，Chrome 打开「从原视频开始学习」页面，
  新安装的 Node 进程持续运行，回环页面返回 200。再点击「选择原视频」，实际观察到原生文件窗口。

本机为 Windows 11 Pro Insider 25H2（10.0.26220.9223），Node 26.10.0、pnpm 10.34.5、
Windows PowerShell 5.1.26100.9223，Chrome 153.0.8010.53，系统缩放保持 150%。

核心修复为 `b13923670ce9b2c4c24a9e05f56b593fd70cf712`，tree 为
`66e1535662af7e9a3a9dc5b10076fc9910204aa0`，分支为 `codex/asbplayer-windows-validation-fixes`。
本机全仓 `pnpm format:check`、`pnpm lint`、指令检查和 diff 检查通过。
该轮 [准确候选 CI](https://github.com/Neil0619/huayi/actions/runs/36035998145) 的 Windows 脚本测试
为 1,152 通过、1 失败、6 跳过；失败发生在原生快捷方式创建或迁移，原错误输出当时未被收集，
尚不能确定 CI 的底层原因。后续产品和浏览器门禁未执行；macOS 作业在最后核对时仍运行中。
此前调度 `36035734991` 使用了错误格式的 `release_id`，在两端候选身份校验阶段失败，未运行测试；
纠正调用参数后验证同一个候选，不修改门禁断言、不将未执行记为通过。

最新候选为 `768c9583a9d1ec778142ac1b37890d7da5172e70`，tree 为
`ae4121d11ad2bbd9acdd3afe9eca19793265abf1`。它让原生回归夹具真正安装旧目录，并在测试中收集
真实 PowerShell 失败输出；保留迁移、实际 `.lnk` 启动、准确配置参数和无关入口保护的全部断言。
安装器仅增加进程启动的测试注入点，默认仍调用真实 `spawn`。本机安装器 3 项测试及定向 lint 通过，
重复安装成功；不能据此把首轮 CI 失败宣告已修复。

截至 2026-09-25 01:57 +08:00，新候选的
[双平台完整 CI 36037649415](https://github.com/Neil0619/huayi/actions/runs/36037649415) 仍运行中。
下一轮须先核对该准确提交及完整日志，Windows 必须实际执行原生快捷方式测试；若再次失败，
使用新增原始错误定位，不跳过测试或弱化断言。该轮未在本机重复整个 `verify:windows`，
不把上一候选本机完整门禁计入本候选。

临时启动探针已移除，旧安装启动脚本已恢复正常；仅保留回滚原件和忽略目录中的脱敏回执。
测试弹出的文件窗口及该次打开器进程已结束。重复桌面启动时产生了新 Node 进程，但用户按 Esc
停止 Computer Use 后未再操作界面，该第二次启动没有新增页面截图通过结论。

100% 新打开器界面仍按用户此前决定暂缓；
本轮没有重做整集声音、字幕和 Provider 学习流程，原媒体流程证据仍归属原候选。
未调用真实付费 Provider、外部词典；未合并 main、部署或发布商店版本。

## 2026-09-25 全面检查的后续修复

`768c958` 的 Windows 作业 `107761631119` 仍失败，新增诊断显示 `WScript.Shell.Save()` 无法保存
中文 `.lnk`。本机在桌面目录加入系统 ANSI 代码页外的字符后复现同类失败（2 通过、1 失败），
不是缺少旧安装目录。改用 Windows `IShellLinkW` / `IPersistFile` 的 Unicode 持久化接口后，
相同原生创建、迁移、真实快捷方式启动与无关入口保护测试通过。

另把实际 Node 可执行文件放入含中文、空格和代码页外字符的工具目录，原启动脚本未启动 Node；
原因是 Windows PowerShell 5.1 默认按本机代码页读取 UTF-8 配置。明确 `-Encoding UTF8` 后，
同一原生回归通过。两次均先执行失败回归再修复，本机打开器相关 10 项通过、0 跳过。
该后续修复的双平台完整门禁、最终候选与合并状态将在最终检查回执中另行记录。
