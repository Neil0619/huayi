# asbplayer Mac Git 接续与真实 YouTube 验证回执

2026-09-24。本轮接续 Windows 候选，未修改产品源码。最初两种无扩展、无请求拦截的 Mac 隔离
浏览器均能播放用户指定的真实 YouTube 视频，但英文字幕响应为空，原生字幕未出现，当时学习
闭环及推荐点击 SPA 未能开始。下方保留这些失败及后续有区分力的对照；没有用合成字幕页面
替代真实站点验收。

同日后续：用户确认已重载日常扩展。原生访客窗口、普通启动 CfT 和显式非零调试端口的 CfT
均出现真实英文字幕。最后一种独立会话继续使用实际 Store 包、合成 Provider 和隔离本机词本，
完成了真实 YouTube 学习流程及推荐点击 SPA 验证，详见后续矩阵。此前空响应不能泛化成所有
Mac／隔离窗口均不可用；本次通过也没有确定历史失败的根因。

## 准确候选与环境

- 仓库：`https://github.com/Neil0619/huayi.git`。
- 输入分支：`codex/asbplayer-windows-validation-fixes`；准确接续提交：
  `bb952f5c3186331f55c932501c799c13a2fdb5ab`。
- 输出分支：`codex/asbplayer-mac-validation`。其回执提交由 Git 历史标识，不将文档 HEAD 说成
  CI 验证过的源码提交。
- 已通过完整门禁的代码候选：`83c1ab0445bcc20e7018f2fdba85c7ee299afe99`；Git tree：
  `f2b63d5cc5be4a3b2afdafdfabf9b710f369340c`。
- 本轮核对 [CI 35920547519](https://github.com/Neil0619/huayi/actions/runs/35920547519)
  的 headSha 与两个 job，Windows/macOS 均成功，各浏览器 243/243。`83c1ab0..bb952f5` 只有四份
  文档变化，本轮也只提交文档；没有无理由重跑相同候选的全量 CI。
- 原 Mac checkout 保留分支 `codex/asbplayer-windows-validation` 和 HEAD
  `c67405c7ff7562e950bf5b03dc46971c2c6f5b24`，原有源码未改写。先核对 origin、工作区和固定包，
  fetch 后创建独立工作树检出准确 SHA；未 reset、clean、合并 main。
- macOS 27.0，build `26A428`，arm64；Node.js 24.18.0、pnpm 10.34.5、Playwright 1.61.1。
  独立工作树执行 frozen-lockfile 安装和 Mac 构建，没有复制 Windows node_modules 或构建文件。
- 安装版 Chrome 153.0.8010.53；Playwright Chrome for Testing 149.0.7827.55。
  真站对照使用有头浏览器、全新临时 Profile、`viewport: null`、1280×900 窗口，实读 DPR 2。

## 最初隔离浏览器对照及限制（历史失败）

目标为用户在任务中提供的 watch 页面。回执不保存完整播放 URL、字幕原文、签名参数、请求头、
账号、媒体文件名或浏览器凭据。只观察状态、响应字节数、语言和有界显示指标。

| 对照                      | 观察                                                                                       | 结论                                    |
| ------------------------- | ------------------------------------------------------------------------------------------ | --------------------------------------- |
| 安装版 Chrome 首次        | 导航阶段 Error，没有取得 net::ERR                                                          | 原失败保留，未到字幕检查；原因未知      |
| 安装版 Chrome，单次补诊断 | 播放器 OK、readyState 4、播放中、非广告；CC ON，en/asr；timedtext 200／0 字节，原生字幕 0  | 字幕前置条件阻塞                        |
| CfT 149 首次版本对照      | 视频可播放，最终 CC OFF，未观察 timedtext                                                  | 非等价对照，不能证明 CC ON 下的字幕结果 |
| CfT 149，就绪门禁对照     | 等待 readyState 4、播放推进超过 2 秒，实读 CC ON；en/asr timedtext 200／0 字节，原生字幕 0 | 同样阻塞                                |

后面三次分别在 2026-09-24 14:44:36–14:45:12、14:45:35–14:46:04、14:46:53–14:47:25
（UTC+8）运行。每次都没有扩展和请求拦截，也没有注入字幕、合成 YouTube 导航事件、安全绕过、
复制日常 Profile 或配置 Provider。初次导航错误未再现，不能追补其网络错误码。CfT 追加一次
对照是为纠正探针的播放器就绪／CC 前置条件，未修改产品代码。

这些结果说明空字幕现象可在语见之外复现，并与 Windows 隔离环境记录相同；不能由此判定具体
网络、站点策略、自动化或隔离配置是根因，也不否定用户报告的 Windows 日常 Chrome 正常情况。
脚本退出 0 仅表示完成观察，回执状态为 `blocked`，不是验收通过。

| 真实 Store + YouTube 验收项目                    | 最初空字幕对照时的状态         |
| ------------------------------------------------ | ------------------------------ |
| 首次进入并接管真实英文字幕                       | 未运行，原生字幕前置条件不满足 |
| 双击、拖选，翻译和解释完成                       | 未运行                         |
| 本机词本收藏、去重                               | 未运行                         |
| 语见暂停归属、关闭恢复、原本暂停保持             | 未运行                         |
| 临时双语快捷键、CC 开关恢复                      | 未运行                         |
| 点击推荐视频触发真实 SPA、旧会话失效、新字幕接入 | 未运行                         |

无扩展对照中读取 CC 状态不等于 Store 的 CC 开关回归通过。没有配置或调用真实／合成 Provider，
没有外部词典写入。诊断浏览器全部关闭，其一次性 Profile 已清理；日常浏览器资料未读取或复制。

原始记录留在本机忽略目录，脱敏结论通过本文件进入 Git。供本机核对的 SHA-256：

| 回执                   | SHA-256                                                            |
| ---------------------- | ------------------------------------------------------------------ |
| 原始导航失败           | `d48d340c23f187fa43463155ccbdec7ac84be709a5c253ec4da6eeb8cfc02cbd` |
| Chrome 153 对照        | `5ece03a80ad8510ed4c57b1bac23d78b709e8d46b56d071b83c44fbd169eb501` |
| CfT 149 首次非等价对照 | `ed327590085c21047c73ce746fd4c180ae9d2467f0a49422d42cb27bd8c5ff8a` |
| CfT 149 就绪门禁对照   | `79026b318f43eadada08ed55ba9586a3d0a2f1f06353545016c1c37523848a62` |

## Mac Hosted 固定目录更新

从准确接续提交的相同代码构建 Hosted 包，仍使用唯一日常路径
`/Users/niuzhenya/Documents/huayi/apps/store-extension/dist`。没有创建第二个日常安装目录，
未从原 checkout 的旧代码重新构建。release 包只保留在隔离工作树作测试用途。

| 检查                                                                                                                                              | 结果与范围                                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| `pnpm install --frozen-lockfile`、四个 Store 共享依赖构建、Store release 构建                                                                     | 通过；Mac 本机产物                                     |
| `pnpm store:local:build`、`pnpm store:local:status`                                                                                               | 通过；Hosted 固定 ID 正确                              |
| `HUAYI_STORE_E2E_PACKAGE_PROFILE=hosted pnpm exec vitest run --project store-extension apps/store-extension/src/packaged-query-streaming.test.ts` | 1/1；实际打包 Worker、合成响应                         |
| `HUAYI_STORE_E2E_PACKAGE_PROFILE=hosted pnpm exec playwright test apps/store-extension/e2e/packaged-query-interaction.spec.ts`                    | 7/7，17.4 秒；实际打包 Content、离线交互页面           |
| 固定目录全部文件与审计包比较                                                                                                                      | 23/23 文件名及 SHA-256 一致                            |
| 隔离 CfT 从固定目录实际加载，外部网络阻断                                                                                                         | 固定 ID 正确，asbplayer 设置可见且默认英文，页面错误 0 |

固定目录加载检查使用 CfT 149.0.7827.55 和新临时 Profile，无 Provider 凭据。首次探针错误地
打开“网站管理”分类，而 asbplayer 设置属于“常用”，可见性检查失败；保留该失败回执。核对
打包 HTML 后，仅修正探针分类并单次复测通过，未修改或重建产品。这是设置页加载检查，不能
代替真实字幕学习或日常浏览器已重载。两次隔离浏览器均已关闭并清理各自临时 Profile。

新旧 Manifest 公钥完全相同，均计算为 `hoijjhgcckfhbcefoclgbhkgninnkknd`，版本仍为 1.0.0。
旧包 21 文件，新包 23 文件，新增两个 asbplayer 入口、八项变化、十三项不变，无删除文件。
更新前确认两端目录及子文件无符号链接；再次检查旧摘要防止并发改写，备份旧程序文件，再逐文件
原子替换，最后更新 Manifest 并核对完整清单。未卸载条目、迁移／清除 storage、重新配对或修改 key。
旧程序备份位于本轮工作树忽略目录 `artifacts/stable-install-mac-20260924/previous-dist`，
同目录 `receipt.json` 记录逐文件新旧摘要。备份不含浏览器数据，不作为源码交接包。

| Hosted 文件     | SHA-256                                                            |
| --------------- | ------------------------------------------------------------------ |
| 更新前 Manifest | `b638157db849d115cb3ce3d07c50cd4262516d25f9058a1a7979f3e72f68df70` |
| 更新后 Manifest | `8c8803f6e9374258a9a032dcc11f6126bff11274f92fc8c9a0fc56c1b2675e91` |
| 更新后 Worker   | `511c58d18872daa31bea8430aaf597263eb8a94703ba1c34d4ee77d4f09fbe6f` |

以上是 Hosted 摘要，不能与诊断记录中另行计算的 dist-release Manifest／Worker 摘要混用。
本轮没有源码变更，也没有重新构建 production 商店包、部署 API/Web 或发布商店版本。

日常 Chrome 详情 URL 和窗口标题已对应预期 ID 的语见，但工具未取得详情页内部加载路径或重载
控件：浏览器连接先报 request-header policy 错误，原生访问详情内容无可用 AX，截图为空白。
此时固定程序目录更新不等于日常条目已重载。随后用户明确回复“已重载”，现记录为
**用户确认重载**；不是代理独立观察到重载成功。日常详情页加载路径仍未由工具核验。

## 后续原生访客窗口对照

2026-09-24 约 15:07–15:09（UTC+8），用户要求代理执行访客检查。代理从已安装 Chrome 的
Profile 菜单选择 `Open Guest Profile`，读到访客欢迎页、窗口 `(Guest)` 及 Guest 按钮，随后
通过地址栏打开任务指定视频。页面显示 Sign in，未登录或导入任何日常浏览器数据。

播放器真实推进至 00:24、00:43，原生字幕区域显示不同英文文本；设置菜单明确为
`Subtitles/CC English (auto-generated)`，CC 开关值为 1，后续播放推进到 01:01。
这项**原生访客英文字幕前置检查通过**。没有连接 Playwright、替换页面／字幕／事件或拦截请求，
也没有测量该次 timedtext 的 HTTP 状态或字节数，不能把此前 200／0 字节赋给本次运行。
检查结束关闭了本次访客窗口，保留原日常窗口；没有运行 Provider 查询或词本写入。

访客窗口不能承载扩展安装，因此这个通过结果不是 Store 学习通过。它只证实同机、该视频在
本次访客会话有真实可用字幕，不能单独确定此前失败是自动化、Profile、启动参数、时序或站点
变化导致。历史失败与未验证列表全部保留，下一步在可加载扩展的隔离 Profile 重新确认字幕。

随后使用现有官方 CfT 149.0.7827.55 的普通进程启动方式作两次有界对照，每次新建临时普通
Profile，加载实际 release 后先在 Chrome 扩展管理页关闭该条目，再打开真实视频；因此网页访问
时扩展为停用状态。全程通过原生 UI 操作，没有 Playwright 控制器连接，也没有复制 Profile：

| 启动条件                                                                       | 观察                                                                                  |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- |
| 启用 localhost 随机远程调试端口（`--remote-debugging-port=0`），尚未连接控制器 | 播放推进至 00:22／00:35，设置选中英文自动字幕，但 CC 描述为 unavailable，未见原生字幕 |
| 不带远程调试参数                                                               | 多次观察到不同英文原生字幕，播放推进至 01:15，设置为英文自动字幕、CC 值 1             |

两次 Profile 和启动时刻不同，不能把结果归因于某个单一参数，也不能由未连接控制器推断不存在
浏览器调试状态影响。本轮没有在这些 UI 对照测量 timedtext 响应。两个自建进程均结束，其临时
Profile 已清理，未配置模型或收藏；这进一步说明 CfT 的原生字幕也可用，但还需在实际查询测试
使用的会话中重验。脱敏 UI 观察摘要 SHA-256 为
`822cde3122f0cb94117d3d73b34d69da733d49fa53714230691f6c0664c73e94`。

## 实际 Store + 真实 YouTube 学习验证

2026-09-24 15:21:23–15:33:48（UTC+8）取得通过回执。使用同一 CfT 149.0.7827.55，普通进程
启动、全新临时普通 Profile、localhost 显式非零调试端口；没有 Playwright 默认启动参数、
伪装自动化状态或绕过安全限制。先通过原生扩展管理页停用实际 release 条目，再在真实视频
播放 00:34／00:36 时观察到变化的英文字幕。连接 CDP 后，约 01:36／02:05 仍有变化的
en/asr 字幕；安装 Provider 请求边界后约 02:15 仍可用。上述相对时间来自不同观察点，
不代表精确的字幕出现时刻。

随后启用实际包，先安装合成响应拦截，再在扩展设置中保存纯测试凭据和同意记录，最后重新进入
真实 watch 文档验证首次接管。只替换已配置的 DeepSeek 查询响应；YouTube 页面、媒体、字幕
和导航响应全部原样放行，没有注入字幕或伪造导航事件。其他非允许目标被阻断，未配置真实模型
凭据、外部词典或云账号。整个运行共 11 次合成 Provider 请求，不涉及真实模型费用或回答质量。

| 验收项目                | 结果与证据                                                                                                                                       |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 首次进入真实 watch 页面 | 非广告、英文自动字幕开启；实际 Store 英文层可见、文本非空                                                                                        |
| 双击单词                | 真实鼠标双击，完成 `translate-word` 和 `explain-word`                                                                                            |
| 拖选短语                | 真实鼠标拖选两个词，完成 `translate-lexical` 和 `explain-lexical`                                                                                |
| 终态与请求数            | 结果类型及相应内容区存在、Stop 隐藏、Retry 为 0；补充测量的单词翻译、解释及 SPA 新查询各增加 1 次请求；已自动完成的拖选结果再次点击翻译增加 0 次 |
| 本机收藏与去重          | 保存到隔离本机词本；准确重复保存前后均为 1 个词条、3 个语境，显示语境已存在状态                                                                  |
| 暂停归属                | 播放中选择后暂停、Escape 关闭后恢复；选择前已暂停则关闭后仍暂停                                                                                  |
| 临时双语                | 配置并初始化 Shift+Z 后，真实中文从隐藏变为可见，按住时暂停，释放后隐藏并恢复原播放                                                              |
| CC 开关                 | 点击实际 CC 关闭后学习层移除，开启后恢复真实英文学习字幕                                                                                         |
| 真实推荐 SPA            | 点击页面推荐链接；导航 start/finish 各 1 次，视频标识摘要改变，document 与 timeOrigin 保持，旧字幕节点断开、旧卡移除、新英文层恰为 1 个          |
| SPA 后继续学习          | 新会话真实双击完成翻译，关闭词卡后恢复播放                                                                                                       |

词条内容来自合成模型样例，选择和语境来自真实字幕；这里验证保存和去重机制，不评价词义正确性。
请求数记录包括交互补测，11 次不是一次选择的请求量；没有把未设置计数起点的步骤补写成单次计数。

以下探针失败保留在同一回执，均未通过修改产品源码消除：

- 一开始选中了 YouTube service worker，改为明确筛选 `chrome-extension://` Worker 后才配置 Provider。
- 重开已收藏卡时错误预期 Save 按钮必须禁用；改为实际重复保存，核对词条、语境数量及重复状态。
- 新设快捷键在控制器初始化时读取，首次尝试未重载；重载后使用普通字母键 `z`，纠正此前
  `KeyZ` 的测试输入。通过的是初始化后的快捷键行为，没有声称本次验证了无需重载的快捷键更新。
- 重载曾进入真实广告，等待英文学习层超时；观察到 ad-showing 和 en-US 原生字幕后，点击真实
  Skip ad 按钮继续目标视频。没有用广告字幕冒充目标视频接管成功。
- 原生工具初次绑定 CfT 意外打开了默认启动窗口；该代理创建的进程在正式隔离会话前已关闭，
  未导航、登录或读取／复制日常 Chrome 资料。扩展管理空白页由新原生标签页解决。

实际加载的 release ID 为 `mefdcofcgjnmpcnhokdpceoljfdachci`，仅用于这个临时 Profile，
不是日常 Hosted ID。来源仍是 `83c1ab0` 的相同产品代码；本轮未重新构建或修改包。

| release 文件    | SHA-256                                                            |
| --------------- | ------------------------------------------------------------------ |
| Manifest        | `535572ce7920400f0f7bb1e87b5f5eb79cbbb776241fac43d970fec15fbe6eed` |
| Worker          | `aedc3909b13c9ccf7ac5baf35df3bb712b2643797366af6a8e8ccc0e9d948c7b` |
| YouTube MAIN    | `5fab35bf246828333c97de246f16da4731b8ea739ab1cb7ced4bc11344564954` |
| YouTube Content | `661af39a6e476e700bc8823d1f3450b6ad1d82788a17bd6c1f2c054d8861b76e` |

脱敏回执摘要为 `8c710f676b67160c5d7adfd3bd94467b98d48b3adbd815bfd916dc9a94976f0d`，保存在
忽略目录 `artifacts/mac-cft-explicit-port-store-validation.json`。主任务复核了 15 项最终验收值、
6 份包文件摘要、6 份结果终态、重复保存计数及真实 SPA 指标；辅助脚本语法检查通过。测试浏览器
进程已退出、临时 Profile 已删除，主任务再次确认两者不存在；没有后台验收继续运行。

这份 Mac 实测补齐了本次真实 YouTube 学习与 SPA 范围，不替代 Windows 实测，不确定历史空响应
的根因，不证明所有 Chrome 启动方式都可用，也不覆盖真实付费模型、外部词典或日常账号业务。

## 保留边界

1. 用户已确认日常扩展重载，不重复要求重载。保持既有 ID／固定路径，不卸载、清空存储或改用
   dist-release；本轮未执行日常账号业务，工具未独立确认日常加载路径。
2. 原生访客的真实英文字幕前置检查，以及独立 Profile 的实际 Store 学习流程，均已由代理验证，
   不再要求用户重复这些操作。
   本轮没有产品修复；共享源码、依赖和包保持不变，沿用准确代码候选已有的双平台检查。
3. 本轮补充检查包含真实站点上述矩阵、产物及备份摘要、辅助脚本语法、文档格式、指令和差异
   检查；未无理由重跑全仓测试、构建或 CI。只有脱敏文档进入 Git，辅助记录不是交接包。
4. 保留 [Windows 回执](asbplayer-windows-validation.md) 的所有历史失败及环境条件。Chrome 154
   官网基础流程依赖显式拒绝本地字体权限和调整弹窗尺寸，扩展矩阵依赖拒绝字体权限；默认全屏／
   自动弹窗失败未解决。共享代码若发生新修复，必须取得新准确候选的对应双平台检查。

本轮文档提交不包含凭据、原始网络响应、字幕或媒体内容；只通过 Git 交接，不合并 main、不部署。
