# 批量预处理与播放器换片验证

2026-09-25，影响 `shared + Windows`。验证候选为
`57f5f9f6c6868600952bc3f5329583a8db2f46b7`，tree
`22d1031e37567b3da66a35e81283fd5a3180b199`，基于 `809875d`，分支
`codex/asbplayer-windows-validation-fixes`。产品实现提交为
`9d4bb728de3d9360f27f9b5a0d4fe42154818b80`（tree
`5500b366e508bc14f0bd794f7072a2da2b7dae1b`）；`687d444` 调整 macOS CI 整体预算，
`57f5f9f` 再修复完整 CI 暴露的原生跳转事件竞态。
本回执提交只改文档。
用户授权验证后合并推送，没有部署或发布商店版本。

## 变更与回归

- 本机打开器新增「批量预处理视频」：原生 Ctrl/Shift 多选，最多 500 个文件，串行准备，
  每项显示进度、成功/失败和缓存位置；单项失败继续，重复项去重，已有有效缓存自动复用。
- 「停止剩余任务」完成当前文件后跳过其余文件；保留已完成缓存。批量结果可直接
  「选用此视频」，以后重新选原文件也会复用缓存。关闭网页不停止本机队列，退出进程后
  列表不保留，但已完成缓存保留。媒体仍在原视频旁的「缓存视频」目录，原文件只读。
- 原「打开另一个视频」只调用 `opener.focus()`，不能保证置前，更不会发起选片。
  实际 Store E2E 点击该按钮后不能进入选片，先验证失败，再改为限定
  origin/source/nonce 的换片请求；仅接受当前成功导入的播放器。忙碌、关闭或无响应均有提示。
- 批量完成后的顶部提示曾残留「请选择文件」，另加浏览器断言复现，修复后转绿。
  早期候选 `df7b7ef` 的本机和 CI 门禁主动中止，不计通过。
- 网页不能传文件路径、工具参数或命令；选用结果只使用本次队列随机 ID，继续校验会话令牌、
  准确 Host/Origin 和空 JSON 请求体。无新增扩展权限、生产依赖、真实 Provider 或词典请求。

## 自动检查

本机完整 `pnpm verify:windows` 于 12:00:39–13:01:53（UTC+8）完成，退出码 0。
从产品提交 `9d4bb72` 启动，运行期间仅新增 CI 预算与说明提交 `687d444`；产品源文件未变，
完整构建后的内容脚本摘要与此前实际媒体候选一致。预算修改另跑治理与平台配置测试 9/9。

- instructions、format、lint、所有 workspace typecheck、architecture、所有 build 通过。
- scripts：1183 项中 1177 通过，6 项平台原语跳过，0 失败。
- 单元测试批次全部通过；Store 1226/1226，API 1302/1302。Native Host 67 项不适用平台测试跳过，
  不把跳过项计为 Windows 实机通过。
- Store coverage 1226/1226；statements 90.37%、branches 85.16%、functions 90.86%、lines 92.44%。
- 完整浏览器 250/250（15.1 分钟），包含普通网页、YouTube、BFCache 和新增批量/换片。
- Store release 审计、安全依赖审计（无已知漏洞）、Windows SEA package/health、diff 检查通过。

前一轮产品提交的 [CI 36092693367](https://github.com/Neil0619/huayi/actions/runs/36092693367)：
Windows 作业通过（浏览器 250/250）；Mac 已通过第 233/250 条浏览器检查时被整个作业的
45 分钟上限取消，GitHub 注释明确为 `The job has exceeded the maximum execution time of 45m0s`。
不计为完整 Mac 通过，也没有断言失败。仅将 Mac 作业总预算增为 60 分钟，单条测试时限、
断言、覆盖率阈值均未变。准确候选 `687d444` 的新 [CI 36096164112](https://github.com/Neil0619/huayi/actions/runs/36096164112)：

| 平台    | 结果                                                                                                  |
| ------- | ----------------------------------------------------------------------------------------------------- |
| Windows | 通过，28 分 32 秒；浏览器 250/250（7.6 分钟），Store 覆盖率与本机一致，安全审计和 SEA health 通过     |
| macOS   | 失败，249/250；用户 seek 后关闭词卡时错误恢复播放，暂停所有权断言失败。安全审计未执行，不计为完整通过 |

随后已用单元测试和实际注册的 Store 确认一个媒体事件竞态：`currentTime` 已变，
`seeking` 事件尚未派发时关闭词卡，旧实现仍会自动恢复播放。新增两条单元回归先失败，
正式 Store 的可信 Escape 同任务回归也先失败；修复记录暂停后的位置，并在恢复前检查
位置和原生 `seeking` 状态，不改变原有断言或等待预算。35 条暂停/交互单测通过，
新旧两条实际 Store 浏览器回归各重复 5 次，10/10 通过。原始 Windows 矩阵此前重复 20 次
均通过，不能因此将 Mac 的已捕获失败视为通过。
最新候选 `57f5f9f` 的 [CI 36099176541](https://github.com/Neil0619/huayi/actions/runs/36099176541)：

| 平台    | 完整门禁结果                                                                                                          |
| ------- | --------------------------------------------------------------------------------------------------------------------- |
| macOS   | 通过，29 分 32 秒；浏览器 251/251（9.1 分钟），原暂停矩阵和新增同任务 seek 回归均通过，覆盖率与本机一致，安全审计通过 |
| Windows | 通过，44 分 46 秒；浏览器 251/251（9.9 分钟），Store 1228/1228 与覆盖率通过，安全审计和 SEA health 通过               |

本机对新增修复运行受影响的 Store 完整覆盖率、asbplayer/YouTube 浏览器回归和安装后实际媒体流程；
先前全量本机结果单独保留，不将它标为 `57f5f9f` 的全量本机门禁。本机 Store 覆盖率运行
1228/1228 通过（571.94 秒），上述四项覆盖率百分比不变；实际构建的 asbplayer/YouTube
定向浏览器回归 18/18 通过（1.8 分钟），包含普通网页划词、BFCache、新增批量和原有/新增
暂停回归。lint、Store 类型检查、构建、指令与格式检查通过。

重点回归：真实 localhost HTTP 串行排队、失败继续、停止、取消保留结果、选用成功项、
拒绝浏览器路径及错误 Origin；原生单选/多选窗口出现和取消、Unicode 文件名边界；
实际注册 Store 导入内容、错误 nonce、忙碌反馈、关闭打开器、旧播放器关闭及批量页面操作。

## 150% 实际官网和两集媒体

Chrome for Testing `154.0.8037.57`，系统实读 DPR `1.5`，浏览器视口 `1266 × 806`。
实际 Store 的 `asbplayer-content.js` SHA-256 为
`b76b108c84a34d98ad11d8d082e5ecd9052aa8ef98675e945b155bbde081f8db`。
官网本地字体权限在此隔离夹具中明确拒绝，不声明官网默认权限设置通过。

用用户影视库的 S09E01 和 S01E01 批量处理：S09E01 复用已有缓存，S01E01 首轮完成
AC3 → AAC 与内嵌文字字幕提取。之后在 `9d4bb72` 重新运行，两集均复用缓存。
S01E01 缓存视频 707197808 字节，S09E01 缓存视频 611074977 字节；各自在原片目录旁。

| 检查          | 结果                                                    |
| ------------- | ------------------------------------------------------- |
| 批量          | 两项成功，未重复转换；原文件大小与修改时间保持不变      |
| 第一集        | 1226.271 秒、1920 宽、1 条音轨，音频解码字节 41449      |
| 第一次导入    | 点击开始学习至视频可播放 3202 ms                        |
| 学习          | 双语中文预选、翻译/解释完成、本地收藏、Esc 恢复播放通过 |
| 换片          | 直接点击播放器按钮选入另一集；上一播放器关闭            |
| 第二集        | 1378.399 秒、1920 宽，音频解码字节 142114               |
| 第二次导入    | 1984 ms，准备缓存检查 14 ms                             |
| HTTP 媒体传输 | 仅两份字幕，分别 152656/30512 字节，没有整视频请求      |
| Provider      | 2 次合成响应，无真实模型收费或外部词典写入              |

第一集完成完整学习，第二集验证导入、换片、播放及音轨解码，没有再次调用翻译/解释。
这些耗时为当次机器和网络条件的观察值；音轨解码不等同于人工整集听音。
原视频选择与目录接口在该浏览器验收中注入，返回真实磁盘 File，正式文件校验和注册内容脚本
未替换。原生目录授权的历史人工证据见上一轮回执，不能据此宣称此次完整原生多选已通过。

## 安装与环境

已备份原构建后更新固定目录：

- 扩展：`E:\Document\huayi\apps\store-extension\dist`，沿用 Hosted acceptance 构建，
  Extension ID `hoijjhgcckfhbcefoclgbhkgninnkknd`；23 个构建文件逐项 SHA-256 与候选一致。
  Manifest 摘要保持 `8c8803f6e9374258a9a032dcc11f6126bff11274f92fc8c9a0fc56c1b2675e91`，
  固定目录的 Hosted Store audit 通过。
- 打开器：`C:\Users\niu06\SeenSaid\asbplayer-opener`，8 个运行模块与源码逐项匹配，
  桌面入口按现有安装路径更新。安装前通过身份验证关闭旧的空闲进程；没有中止媒体处理。
  `config.json` 和持久浏览器 origin 身份均保持不变。

在 `687d444` 上，使用安装后扩展文件的隔离快照及直接从安装目录导入的打开器模块，
再次通过实际官网两集导入、首集完整学习和换片：首次导入 3285 ms、换片导入 1225 ms，
第二次缓存检查 9 ms；
音频解码字节分别 41973/130632，所有原片大小和修改时间不变，4 次准备全部复用缓存，
两次合成 Provider 请求，无整视频 HTTP 请求。仍为系统实读 150%，原生选择接口注入，
不能替代未完成的手工多选。隔离快照只用于验收，日常扩展加载目录始终不变。
另用安装后的打开器检查 150% 页面布局：两集长文件名和缓存路径可换行，页面无水平溢出
（视口宽 1266、文档宽 1251），批量结果保留独立滚动区域；截图仅留在本机私有回执中。

最终 `57f5f9f` 再次更新同一固定扩展目录，23 个文件与 Hosted 构建逐项匹配，
Manifest/扩展 ID 仍不变，8 个打开器模块仍与候选一致，本机配置摘要保持不变。
最终 `asbplayer-content.js` SHA-256 为
`521e4c9804782cda7e2a241abaf21299a4d450dcf68b9428d85fa56a415481b1`。

使用最终安装文件的隔离快照再次验证两集：第一集导入 2709 ms，完成翻译、解释、本地保存和
Esc 恢复播放；第二集换片导入 1347 ms，缓存检查 7 ms，旧播放器关闭、播放和音频解码通过。
两集音频解码字节分别 39645/140498，原片大小与修改时间不变，4 次准备都复用缓存，
仍只有 2 次合成 Provider 请求和两份字幕 HTTP 请求。原生选片/目录接口的注入边界保持不变。
此时工作树仅有本回执文档未提交，`apps/`、`scripts/` 与准确候选无差异；不虚称整个工作树干净。

使用更新需要在 `chrome://extensions` 重载语见，关闭旧播放器页面，再双击桌面
「语见本机视频」。此次没有操作日常 Chrome 的已加载扩展会话，不能将文件更新称为该会话已重载。

环境：Windows 11 Pro Insider Preview 25H2 `10.0.26220.9223` x64、PowerShell `7.6.5`、
Node `26.10.0`、pnpm `10.34.5`、Git `2.45.1.windows.1`、Playwright `1.61.1`，
FFmpeg `6.1.1`、ffprobe `4.0.2`；媒体盘 NTFS。日常 Chrome 当次为 `153.0.8010.54`。

## 未验证与接续

- 完整原生多选曾尝试，但未完成。桌面工具在重新枚举窗口后仍连续两次报告
  `window id … no longer belongs to Chrome; current owner is Chrome`；没有成功选择任何文件。
  仅清理了该次测试拥有的文件窗口、浏览器和进程，保存失败回执，没有将其计为应用通过。
  窗口真实出现/取消测试和注入路径的媒体流程分别记账。
- 新打开器 100% 系统缩放按用户此前决定暂缓；150% 不能代替 100%。
- macOS 新共享换片行为尚未做实机 Chrome 验收；CI 不代替实机，原生打开器目前仅支持 Windows。
- 真实 YouTube 临时中文此前受上游 HTTP 429 阻塞，本轮没有声称该上游问题消失。
- 图像字幕仍需另选文字字幕，视频编码支持范围没有扩展，不承诺影视库所有格式都能播放。

Git 接续后先运行 `pnpm install --frozen-lockfile`，定向检查为
`node --test scripts/asbplayer-opener-batch.test.mjs scripts/asbplayer-opener-server.test.mjs scripts/asbplayer-open.test.mjs scripts/install-asbplayer-opener.test.mjs`，
浏览器回归为 `pnpm exec playwright test apps/store-extension/e2e/asbplayer-local-import.spec.ts`，
完整 Windows 命令为 `pnpm verify:windows`。原生交互补验：批量多选两集、取消选择、
处理中停止剩余任务、选择完成项学习，再从播放器按钮换另一集，确认文件窗口实际置前。
